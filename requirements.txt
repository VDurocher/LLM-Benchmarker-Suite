# Production dependencies — LLM-Benchmarker-Suite
# Python 3.11+ required

# NLP processing and TF-IDF vectorisation
scikit-learn>=1.4.0

# JSON Schema validation (Draft-7)
jsonschema>=4.21.0

# Matrix computation (scikit-learn dependency)
numpy>=1.26.0

# Live inference and LLM-as-a-judge — optional depending on the provider used
# Install only the provider you need:
openai>=1.78.0
anthropic>=0.49.0
